// MUI
import { Box } from '@mui/material'

// COMPONENTS
import EthereumAuth from '@/components/Auth/EthereumAuth'

const Auth = () => {
  return (
    <Box sx={{ width: '100%' }}>

        <EthereumAuth />
    </Box>
  )
}

export default Auth
